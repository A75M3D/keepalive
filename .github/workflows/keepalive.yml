name: Supabase Keep Alive

on:
  schedule:
    - cron: "0 */12 * * *"   # تشغيل كل 12 ساعة
  workflow_dispatch:         # تشغيل يدوي لو عايز

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Curl Supabase Endpoint
        run: |
          curl -I https://sjipwstkvvrautexigmt.supabase.co/rest/v1
